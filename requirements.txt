# Python dependencies for Warner Tracker

# Web UI
Flask>=2.3.0
gunicorn>=21.2.0

# Database (Postgres on Railway)
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# Excel file generation
openpyxl>=3.0.0

# HTTP requests for album art fetching
requests>=2.31.0

# TikTok scraping
# Pinned: TikTok extractor changes weekly and an unpinned `pip install yt-dlp`
# means every Docker rebuild can change scraper behavior silently. Bump
# this version intentionally when you've verified a newer one works.
yt-dlp==2026.3.17
# curl_cffi enables yt-dlp's --extractor-args impersonation_target=chrome,
# which spoofs Chrome's TLS fingerprint to bypass TikTok's bot detection.
curl_cffi>=0.7.0

# Instagram scraping
instaloader>=4.10.0

# Retry logic with backoff
tenacity>=8.2.0

# Progress bars
tqdm>=4.66.0

# Development tools (optional)
# watchdog>=3.0.0  # For file watching (uncomment if needed)

# CORS support
flask-cors>=4.0.0

# Slack bot (Events API integration)
slack-bolt>=1.18.0

# Anthropic Claude API (LLM parsing for Slack messages)
anthropic>=0.40.0

# Apify (TikTok scraping)
apify-client>=2.5.0

# Scheduled jobs (daily cron)
APScheduler>=3.10.0

