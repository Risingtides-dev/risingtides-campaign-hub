# Backend API base URL (empty = same-origin in prod, localhost:5055 in dev)
VITE_API_URL=

# Base URL of the TidesTracker app hosting the internal overview board
VITE_TRACKER_BASE_URL=https://risingtides-tracker.com

# Embed key for the TidesTracker internal overview board (never commit the real key)
VITE_TRACKER_EMBED_KEY=
